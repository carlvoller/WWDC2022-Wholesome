import SwiftUI

struct Page3: View {
    
    var body: some View {
            VStack {
                Text("🦠 ⚔️")
                    .font(.system(size: 80))
                    .padding(.bottom)
                Text("In difficult times like during a global pandemic, or an outbreak of war, it can cause many of us to feel especially hopeless.")
                    .font(.system(size: 25))
                    .padding(.horizontal)
                    .multilineTextAlignment(.center)
            }
    }
}
